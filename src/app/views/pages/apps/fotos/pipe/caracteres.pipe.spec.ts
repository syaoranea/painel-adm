import { CaracteresPipe } from './caracteres.pipe';

describe('CaracteresPipe', () => {
  it('create an instance', () => {
    const pipe = new CaracteresPipe();
    expect(pipe).toBeTruthy();
  });
});
